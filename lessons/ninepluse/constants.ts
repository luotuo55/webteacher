
import { Stage } from './types';

export const STAGE_ORDER = [
  Stage.WELCOME,
  Stage.WARMUP,
  Stage.SCENARIO,
  Stage.METHOD,
  Stage.TRY,
  Stage.PATTERN,
  Stage.PRACTICE,
  Stage.SUMMARY
];

export const COLORS = {
  primary: 'bg-green-500',
  secondary: 'bg-yellow-400',
  accent: 'bg-blue-500',
  text: 'text-slate-800'
};
